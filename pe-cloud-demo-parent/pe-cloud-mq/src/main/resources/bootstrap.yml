server:
  port: 8004

spring:
   application:
      name: pe-cloud-mq # 应用名称
eureka:
  client:
    registryFetchIntervalSeconds: 5
    service-url:
      defaultZone: ${ESUREKA_SERVICE_URL:http://localhost:8761}/eureka/